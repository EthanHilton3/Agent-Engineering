Generate a python program that simulates the playing of connect 4 with a visual console gameboard. There should be two modes of play: 1) single player, where one human player can play against the program itself, and 2) where two human players can play on the same machine and running instance of the program. Make the playing experience enjoyable for the player and the interface easy to use.

Return a suggested filename (e.g. connect4game.py), a one-paragraph description of the program, whether each mode is implemented, the full Python source code, and state that the file type is py.
